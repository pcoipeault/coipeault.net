{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Building list</h1>

    {{ include ('CoipeaultCNMegapolisBundle:Default:user_service.html.twig', {route: 'building_new'}) }}

    <div class="table-responsive">
        <table class="table table-bordered table-striped records_list">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Location</th>
                    <th>Status</th>
                    <th>Createdat</th>
                    <th>Updatedat</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for entity in entities %}
                <tr{% if entity.status == 1 %} class="success"{% endif %}>
                    <td>{{ entity.name }}</td>
                    <td>{{ entity.location }}</td>
                    <td style="text-align: center;vertical-align: middle;">{% if entity.status == 1 %}<span class="glyphicon glyphicon-ok"> </span>{% endif %}</td>
                    <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                    <td>{% if entity.updatedAt %}{{ entity.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                    <td>
                        {{ include ('CoipeaultCNMegapolisBundle:Default:user_actions.html.twig', {route_base: 'building'}) }}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    
    {{ include ('CoipeaultCNMegapolisBundle:Default:user_service.html.twig', {route: 'building_new'}) }}
    
    {% endblock %}
